name: "infra - ec2 - plan"

on:
  workflow_dispatch:

jobs:
  terraform:
    uses: ./.github/workflows/reuse__terraform-plan.yaml
    secrets: inherit
    with:
      config-directory-name: "infrastructure/ec2"
