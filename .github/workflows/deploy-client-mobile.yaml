name: Deploy client-mobile

run-name: Deploy client-mobile version ${{ github.ref_name }}

on:
  push:
    tags:
      - "@quoll/client-mobile@*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        shell: bash
        run: echo "Deploying client-mobile..."
