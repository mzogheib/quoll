name: "OIDC - destroy"

on:
  workflow_dispatch:

jobs:
  oidc:
    name: "OIDC"
    uses: ./.github/workflows/terraform-destroy.yaml
    with:
      config-directory-name: "./infrastructure"
    secrets: inherit
